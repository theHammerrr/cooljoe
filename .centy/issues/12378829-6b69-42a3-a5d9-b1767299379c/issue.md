# Refactor: Decompose CopilotChat into hooks and presentational components

Split transport/state logic from rendering in the web copilot UI (e.g., useCopilotChat/useQueryExecution + smaller UI components).
