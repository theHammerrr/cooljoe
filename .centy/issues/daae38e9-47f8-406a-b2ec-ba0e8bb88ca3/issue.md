# Refactor: Move allowlists and provider options into validated config module

Replace hardcoded runtime settings with environment-backed, startup-validated configuration for allowlists, AI provider selection, and model names.
